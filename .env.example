# Copy this file to `.env` and adjust values as needed.
# Used by the Prisma CLI for `db:migrate`, `db:seed`, and `db:studio`.
# The API server itself resolves the database path from the Prisma adapter.
DATABASE_URL="file:./dev.db"
